# ⚠️ مهم: يجب تشغيل Migration لجدول contacts

## 🔴 المشكلة

جدول `contacts` غير موجود في قاعدة البيانات، مما يسبب خطأ عند الوصول إلى لوحة الإدمن.

## ✅ الحل

### تشغيل Migration:

```bash
php artisan migrate
```

أو إذا كنت تستخدم MySQL/MariaDB:

```bash
php artisan migrate --force
```

## 📋 Migration المطلوب

الملف: `database/migrations/2025_01_21_110000_create_contacts_table.php`

هذا Migration سينشئ جدول `contacts` مع جميع الحقول المطلوبة.

## 🔍 التحقق من Migration

بعد تشغيل `php artisan migrate`، يمكنك التحقق:

```bash
php artisan migrate:status
```

يجب أن ترى `2025_01_21_110000_create_contacts_table` في قائمة Migrations المنفذة.

## ✅ بعد تشغيل Migration

بعد تشغيل Migration بنجاح:
- ✅ لوحة الإدمن ستعمل بدون أخطاء
- ✅ صفحة `/admin/dashboard` ستعمل
- ✅ صفحة `/admin/contacts` ستعمل
- ✅ إحصائيات رسائل التواصل ستظهر

## 📝 ملاحظة

تم إضافة معالجة للأخطاء في `AdminController` و `admin/layout.blade.php` لتجنب الأخطاء إذا لم يكن الجدول موجوداً، لكن يجب تشغيل Migration لإكمال النظام.

---

**تاريخ:** 2025-01-21

