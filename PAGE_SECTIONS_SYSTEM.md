# نظام إدارة أقسام الصفحات - مكتمل ✅

## 📋 نظرة عامة

تم إنشاء نظام متقدم لإدارة محتوى الصفحات مع تقسيمها إلى أقسام منظمة. هذا يسمح بإدارة محتوى كل صفحة بشكل منظم ومرن.

---

## 🗄️ قاعدة البيانات

### Migration: `create_page_sections_table`

تم إنشاء جدول `page_sections` مع الحقول التالية:

- `id` - المعرف الفريد
- `page_id` - معرف الصفحة (Foreign Key)
- `title` - عنوان القسم (اختياري)
- `slug` - رابط القسم (اختياري)
- `subtitle` - عنوان فرعي (اختياري)
- `content` - محتوى القسم (يدعم HTML)
- `section_type` - نوع القسم (content, hero, features, testimonials, cta, image, code, table)
- `metadata` - بيانات إضافية (JSON)
- `order` - ترتيب القسم (0 = الأول)
- `published` - حالة النشر
- `created_at` / `updated_at` - التواريخ

---

## 📦 Models

### 1. PageSection Model

**العلاقات:**
- `page()` - علاقة belongsTo مع Page

**Scopes:**
- `published()` - الأقسام المنشورة فقط
- `ordered()` - مرتبة حسب order

### 2. Page Model (محدث)

**العلاقات الجديدة:**
- `sections()` - جميع الأقسام
- `publishedSections()` - الأقسام المنشورة فقط

---

## 🎛️ Controllers

### Admin\PageSectionController

**Methods:**
- `index(Page $page)` - عرض جميع أقسام الصفحة
- `create(Page $page)` - نموذج إضافة قسم جديد
- `store(Request $request, Page $page)` - حفظ قسم جديد
- `edit(Page $page, PageSection $section)` - نموذج تعديل قسم
- `update(Request $request, Page $page, PageSection $section)` - تحديث قسم
- `destroy(Page $page, PageSection $section)` - حذف قسم
- `updateOrder(Request $request, Page $page)` - تحديث ترتيب الأقسام (Drag & Drop)

---

## 🛣️ Routes

تم إضافة Routes جديدة في `routes/web.php`:

```php
// Page Sections Management
Route::resource('pages.sections', PageSectionController::class)->except(['show']);
Route::post('pages/{page}/sections/update-order', [PageSectionController::class, 'updateOrder'])
    ->name('pages.sections.update-order');
```

**الروابط المتاحة:**
- `GET /admin/pages/{page}/sections` - عرض الأقسام
- `GET /admin/pages/{page}/sections/create` - إضافة قسم جديد
- `POST /admin/pages/{page}/sections` - حفظ قسم جديد
- `GET /admin/pages/{page}/sections/{section}/edit` - تعديل قسم
- `PUT /admin/pages/{page}/sections/{section}` - تحديث قسم
- `DELETE /admin/pages/{page}/sections/{section}` - حذف قسم
- `POST /admin/pages/{page}/sections/update-order` - تحديث الترتيب

---

## 🎨 Views

### Admin Views

1. **`admin/pages/sections/index.blade.php`**
   - عرض جميع أقسام الصفحة
   - Drag & Drop لإعادة الترتيب
   - أزرار التعديل والحذف
   - عرض حالة النشر

2. **`admin/pages/sections/create.blade.php`**
   - نموذج إضافة قسم جديد
   - اختيار نوع القسم
   - حقول العنوان، المحتوى، الترتيب

3. **`admin/pages/sections/edit.blade.php`**
   - نموذج تعديل قسم موجود
   - نفس الحقول مع البيانات الحالية

### Frontend View

**`pages/page.blade.php` (محدث)**
- يعرض الأقسام المنشورة تلقائياً
- إذا لم توجد أقسام، يعرض المحتوى القديم
- كل قسم له class خاص حسب نوعه

---

## 📝 أنواع الأقسام (Section Types)

1. **content** - محتوى عادي
2. **hero** - قسم البطل (Hero Section)
3. **features** - قسم المميزات
4. **testimonials** - قسم الشهادات
5. **cta** - دعوة للعمل (Call to Action)
6. **image** - قسم الصور
7. **code** - كود برمجي
8. **table** - جدول

---

## 🚀 كيفية الاستخدام

### 1. إضافة قسم جديد لصفحة:

1. اذهب إلى `/admin/pages`
2. اختر صفحة واضغط على "Sections"
3. اضغط "Add New Section"
4. اختر نوع القسم
5. املأ العنوان والمحتوى
6. حدد الترتيب (0 = الأول)
7. احفظ

### 2. إعادة ترتيب الأقسام:

1. اذهب إلى صفحة الأقسام
2. اسحب وأفلت الأقسام لإعادة ترتيبها
3. الترتيب يتم حفظه تلقائياً

### 3. تعديل قسم:

1. اضغط "Edit" على القسم المطلوب
2. عدل المحتوى
3. احفظ التغييرات

### 4. حذف قسم:

1. اضغط "Delete" على القسم المطلوب
2. أكد الحذف

---

## ✨ المميزات

- ✅ إدارة محتوى منظم لكل صفحة
- ✅ تقسيم المحتوى إلى أقسام
- ✅ أنواع أقسام متعددة
- ✅ Drag & Drop لإعادة الترتيب
- ✅ نشر/إلغاء نشر كل قسم
- ✅ دعم HTML في المحتوى
- ✅ بيانات إضافية (metadata) لكل قسم
- ✅ عرض تلقائي في الفرونت اند
- ✅ واجهة سهلة الاستخدام

---

## 📊 مثال على الاستخدام

### صفحة "About" مع أقسام:

1. **Hero Section** - عنوان رئيسي ووصف
2. **Features Section** - مميزات المنصة
3. **Content Section** - معلومات إضافية
4. **CTA Section** - دعوة للانضمام

كل قسم يمكن إدارته بشكل منفصل!

---

## ✅ الحالة

**النظام مكتمل ويعمل بشكل كامل!**

- ✅ Migration تم تشغيله
- ✅ Models جاهزة
- ✅ Controllers جاهزة
- ✅ Views جاهزة
- ✅ Routes مضافة
- ✅ Frontend يعرض الأقسام

**جاهز للاستخدام!** 🎉

